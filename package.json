{
  "name": "afrixchange",
  "private": true,
  "version": "1.0.0",
  "description": "AfriXchange — Regulated crypto brokerage (Kenya MVP)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/api",
    "build": "npm run build --workspace=apps/api",
    "lint": "eslint ."
  }
}

